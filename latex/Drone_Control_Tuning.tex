\documentclass[a4paper,11pt]{scrartcl}

% Language and citation support
\usepackage[english]{babel}
\usepackage{csquotes}

% Load consolidated formatting from labstyle
\usepackage{labstyle}

% Bibliography setup
\usepackage[
  backend=biber,
  style=numeric,
  sorting=none
]{biblatex}
\addbibresource{Zotero_zitieren.bib}

% PDF inclusion (for title page)
\usepackage{pdfpages}

% Document metadata
\renewcommand{\labtitle}{Drone Control Tuning}
\renewcommand{\labsubtitle}{Development of an Offline Betaflight Drone Control Tuning tool}
\renewcommand{\labauthor}{Yuri Bianchi, Dario Jurietti and Janick Dort}
\renewcommand{\labdate}{\today}

\begin{document}

\begin{titlepage}
	\includepdf[pages=1]{pdf/Titelblatt.pdf}
\end{titlepage}

\section{Summary}

Dieses Projekt befasst sich mit der Entwicklung eines Werkzeugs zur Offline-Abstimmung von Gyroskop-Reglern für FPV-Racing-Drohnen auf Basis der Open-Source-Flugsteuerungssoftware Betaflight. Die Qualität des Gyro-Regelkreises beeinflusst direkt die Flugstabilität, die Unterdrückung von Störungen wie Propwash sowie die Wendigkeit der Drohne. Bisher erfolgt die Abstimmung der Drehraten-Regler meist durch zeitaufwendiges Ausprobieren. \par
Ziel des Projekts ist die Entwicklung einer modularen Open-Source-Lösung zur Offline-Optimierung auf Grundlage eines bestehenden Funktionsmusters. Die Umsetzung erfolgt in MATLAB und wird durch eine schlanke Python-Version ergänzt. Zusätzlich werden eine ausführliche Dokumentation im Markdown-Format sowie Beispielanwendungen auf GitHub bereitgestellt. Das Projekt soll der Gemeinschaft eine einfache Möglichkeit bieten, Drohnenparameter effizient zu optimieren und dadurch die Flugleistung zu verbessern. \par
Die Ergebnisse werden in einem öffentlich zugänglichen GitHub-Repository veröffentlicht und in einem Bericht dokumentiert. Neben der Softwareentwicklung beinhaltet das Projekt auch praktische Versuche mit FPV-Drohnen zur Überprüfung der entwickelten Methoden.

\section{Abstract}
This project focuses on the development of a tool for offline tuning of gyro controllers for FPV racing
drones based on the open-source firmware Betaflight. The quality of the gyro control loop has a direct influence
on flight stability, the suppression of disturbances such as prop wash, and the agility of the drone. Until now,
rate controller tuning has mostly been performed through time-consuming trial and error. \par
The aim of the project is to develop a modular, open-source solution for offline tuning based on an existing
proof of concept. The code will be implemented in MATLAB, accompanied by a lightweight Python version, and
supplemented with detailed Markdown documentation and examples on GitHub. The project aims to provide the
community with a simple way to efficiently optimise drone parameters and thereby improve flight performance.
\par
The results will be published in a public GitHub repository and documented in a report. In addition to the
software development, the project also includes practical work with FPV drones to validate the developed
methods.

\newpage

\section{Preface}
This document summarizes the results of our project thesis, which we worked on during the fall semester 2025 at
the ZHAW School of Engineering. For us as systems engineering students, this project provided an ideal
opportunity to apply advanced control theory and signal processing methods to a real, highly dynamic system. FPV
drones present an interesting engineering challenge due to their highly dynamic behaviour and the need for
precise control. This motivated us to investigate mathematical modeling, control engineering aspects, and
offline tuning methods aimed at improving flight performance. \par
Our motivation was to translate the theoretical control concepts we learned over the past three years to a
real, highly dynamic system. We also aimed to develop a tool that not only meets engineering standards but also
provides real value to the FPV community by replacing subjective tuning with objective data analysis. \par
This work was carried out at the Institute of Mechatronic Systems (IMS) at the ZHAW School of Engineering. We
would like to express our gratitude to our supervisor, Michael Peter, for his guidance and support throughout
this project. His expertise, constructive feedback, and willingness to take the time to assist us in developing
complex algorithms were invaluable.

\newpage

\tableofcontents

\newpage

\section{Introduction}
First-person-view (FPV) racing drones require precise and responsive control to achieve high flight stability, minimize disturbances such as propeller wash, and maintain agility during rapid maneuvers. Proper tuning of the angular rate controllers is essential to this performance, as it directly determines how the drone reacts to pilot inputs and external influences. Until now, tuning these controllers has predominantly relied on iterative trial-and-error methods. This approach is both time-intensive and requires substantial piloting experience, thus presenting significant barriers for less experienced pilots.

Betaflight, an open-source firmware widely used in the FPV community, provides a flexible platform for configuring flight controllers. However, despite its popularity, efficient tuning remains a significant challenge. To address this, the present project builds upon an existing proof of concept for offline tuning of angular rate controllers using flight data and system identification techniques. The objective is to transform this concept into a robust, modular, and open-source library that simplifies the tuning process.

The library is implemented in MATLAB and Python, providing tools for analyzing flight logs, estimating system dynamics, and optimizing controller parameters without requiring repeated flight tests. Comprehensive documentation is developed and published on GitHub to ensure accessibility for the FPV community. This documentation demonstrates the practical application of the tool and provides detailed technical background on the underlying methods.

Beyond software development, this project also encompasses the construction and flight testing of an FPV drone to validate the effectiveness of the proposed methods. By providing a structured, data-driven approach to tuning, this work aims to improve flight performance and reduce the barriers to achieving optimal control settings for both hobbyists and professionals.

This document is structured as follows: Section 5 provides the theoretical foundations of control systems and signal processing relevant to the tuning process. Section 6 details the methodological challenges and solutions developed during implementation, including simulations, drone construction, and the code migration from MATLAB to Python. Section 7 presents the results obtained from simulations and flight tests. Finally, Section 8 discusses the findings and outlines future work planned for continuation in the bachelor thesis.

\subsection{Initial situation}
The open-source firmware Betaflight is the most widely used platform for FPV racing drones and is continuously developed by a large community. The quality of the rate control is crucial for flight performance, as it enables stability, agility, and effective suppression of disturbances such as prop wash.\par
Today, tuning of controller parameters is still largely performed by trial-and-error adjustments, a method that is time-consuming and often challenging for less experienced pilots. A proof of concept (developed by Michael Peter) already exists, which enables offline tuning of rate controllers using flight measurement data obtained from chirp signal excitation. However, the current implementation is based on MATLAB, which is not widely used within the FPV community and therefore limits accessibility. In addition, the proof-of-concept requires a certain level of technical knowledge to operate, which further complicates its practical use for many pilots. \par
As a result, the potential of data-driven controller tuning remains largely unused in everyday FPV practice. This creates a gap between academically established system identification and control design methods on the one hand, and the predominantly experience-based tuning approaches used by pilots on the other. Bridging this gap represents a key motivation for the further development of the existing proof of concept.
\newpage

\subsection{Task and Objective}
The aim of this project is to further develop the existing proof of concept into an accessible and practical tool for tuning rate controllers on Betaflight-based FPV drones. The focus lies on transforming the prototype from a research-oriented MATLAB implementation into a solution that can be used by the wider community with minimal technical barriers. This includes designing a clean and modular software architecture, improving usability, and integrating functions for importing, processing, and analyzing flight data.
\par
In addition, the tool will be accompanied by comprehensive Markdown documentation, including explanations of key methods, usage instructions, and practical examples to support new users. The implementation will serve as a foundation for future developments, such as extended analysis features, alternative controller designs, or deployment in other embedded flight control systems. The overarching objective is to provide a reliable, data-driven workflow that replaces subjective trial-and-error tuning and enables verifiable control performance improvements.

\newpage

\section{Theory}

\subsection{Control System and Signal Processing}
This section provides an overview of the key concepts and methods used for the analysis and tuning of the Betaflight flight controller. It introduces the essential aspects of signal processing, control system identification, and evaluation techniques that form the basis for optimizing controller performance. A more detailed description of the underlying theory and mathematical formulation is available in the project documentation on \href{https://github.com/InsaneBroccoli/bf_controller_tuning/tree/PA_final/markdown}{GitHub}.

\subsubsection*{Betaflight Gyro Control System}

Based on the Betaflight source code, the gyro control system was reconstructed to obtain a detailed internal representation of the controller structure. This reconstruction forms the basis for the analysis, as it allows a clear understanding of how measured signals are transmitted through the control loops and how individual controller components influence the system dynamics.

For clarity, the control architecture is illustrated in a simplified form in Figure \ref{fig:bf_gyro_control}. The actual implementation includes additional signals, filters, and controller elements that are not explicitly shown. Nevertheless, the simplified representation captures the essential structure required for analysis.

The control system consists of two loops: an inner loop responsible for angular rate control based on gyroscope feedback, and an outer loop performing angle control based on pilot stick inputs. Both loops incorporate filtering and control elements that directly affect the frequency response and stability characteristics of the system.

\begin{figure}[h]
	\centering
	\includegraphics[width=1.0\textwidth]{./Images/Theory/Regelstrecke.png}
	\caption{Simplified representation of the Betaflight gyro control system}
	\label{fig:bf_gyro_control}
\end{figure}

\newpage

\subsubsection*{Chirp Concept}
A chirp is an input signal that sweeps continuously over a defined frequency range (e.g., from low to high frequency).
This signal excites the system at multiple frequencies in one run, thus allowing the observation of the frequency response and step response,
which form the foundation of controller tuning.
Mathematically, a chirp signal can be represented as a sinusoidal wave with instantaneous frequency that varies linearly or exponentially with time \parencite{bf_chirp}.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.9\textwidth]{./Images/Theory/chirp_signal.png}
	\caption{Chirp signal example}
	\label{fig:chirp}
\end{figure}

\subsubsection*{Chirp Implementation in Betaflight}
For drone flight controller tuning, an exponential frequency sweep is employed.
This choice is motivated by the dominant low-frequency dynamics of multirotor systems, where low frequencies exhibit more predictable responses.
The exponential sweep allocates proportionally more time to higher frequencies, providing better resolution of system behavior in that range.
Additionally, multirotor vibration resonances are typically spaced logarithmically,
making exponential sweeps particularly well suited for efficient excitation across the frequency range.

The measurement process involves three main steps:
\begin{enumerate}
	\item \textbf{Signal injection:} The chirp signal is added to the pilot's stick input, forming the composite setpoint for the PID controller.
	\item \textbf{System response:} The drone attempts to track this rapidly varying setpoint. The motors react, causing the drone to oscillate at frequencies corresponding to the instantaneous chirp frequency.
	\item \textbf{Data logging:} Betaflight's Blackbox feature records high-resolution data from the gyroscope and motor outputs throughout the chirp excitation. This data captures the system response to control inputs across the entire frequency range.
\end{enumerate}
\vspace{1em}

The recorded log file, containing both the input chirp and the corresponding gyroscope response,
is then used for offline analysis to determine the frequency response of the system.
However, to ensure reliable frequency response estimation, the chirp signal must first be properly conditioned before injection into the control loop.

\subsubsection*{Lead-Lag Filter for Chirp Signal Shaping}

Before the chirp signal is injected into the control loop, it is shaped by a lead-lag filter to ensure uniform excitation across the frequency range of interest. This preprocessing step is essential because the closed-loop rate control system exhibits differentiating behavior at low frequencies, which would otherwise lead to negligible motor response at the start of the chirp and potential saturation at higher frequencies.

The lead-lag filter is configured with a pole at 3 Hz and a zero at 30 Hz. This configuration pre-emphasizes low-frequency components while maintaining unity gain at higher frequencies. The frequency response exhibits three distinct characteristics. Below 3 Hz, the filter amplifies the signal to compensate for the system's low-frequency attenuation. Between 3 Hz and 30 Hz, the filter provides a relatively flat response with phase lead. Above 30 Hz, the filter approaches unity gain as the system no longer requires compensation.

The Betaflight rate control loop behaves approximately as a differentiator below 30 Hz when viewed from setpoint input to motor output. Without the lead-lag filter, a chirp starting at 0.2 Hz would produce almost no motor response initially, followed by rapidly increasing amplitude that could saturate the motors. The lead-lag filter compensates for this behavior by shaping the chirp signal such that, when combined with the system's inherent dynamics, a uniform excitation amplitude is achieved across the entire frequency sweep. This ensures sufficient excitation at low frequencies for accurate system identification, prevents motor saturation at higher frequencies, and maintains consistent signal-to-noise ratio throughout the measurement.

The specific corner frequencies of 3 Hz and 30 Hz are configured to match the typical dynamics of FPV racing drones and ensure robust excitation without causing instability or excessive motor stress. In the Betaflight implementation, the filtered chirp signal is added to the pilot's stick input before entering the PID controller, allowing the system to be excited while maintaining manual control authority during flight.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.9\textwidth]{./Images/Theory/Chirp_lead_lag.png}
	\caption{Chirp signal example}
	\label{fig:chirp}
\end{figure}

\subsubsection*{Data Import and Preprocessing}
To analyze Betaflight Blackbox logs effectively, the recorded data must first be imported and preprocessed to ensure correct scaling, structure, and suitability for further analysis. Each log consists of a header containing essential metadata, such as filter settings and PID values, and a data block with the recorded signals. Both components are required for correct interpretation of the flight data.

To reduce computational effort, the raw CSV log files are converted into the faster MATLAB \texttt{.mat} format, allowing for significantly shorter loading times. During this step, signal names are automatically mapped to their corresponding data columns, which simplifies the identification and use of individual signals in subsequent analyses \parencite{bf_LoadData}. Betaflight's High Resolution Mode scales selected signals by a factor of ten to optimize storage and reduce numerical rounding errors. These signals are therefore rescaled to their original values during preprocessing to ensure accurate results \parencite{bf_UnshigRes}.

Finally, the time information, originally stored as a microsecond counter, is converted into a uniform time base. By evaluating the time differences between samples, irregular sampling caused by dropped frames or variations in the effective sampling rate can be detected. Proper handling of the time data is essential for reliable frequency analysis and controller tuning \parencite{bf_TimeConv}.

\subsubsection*{Spectrum and Spectrogram Analysis}

Spectrum and spectrogram analysis are powerful tools for understanding how a system behaves across different frequencies. The spectrum decomposes a signal into its frequency components, indicating which frequencies are dominant and which are attenuated. This allows resonances, noise contributions, and the effectiveness of filtering stages to be identified.

The recorded data logs provide two types of gyroscope signals: the signal \texttt{gyroADC} and the signal \texttt{gyroUnfilt}. The difference between these two signals is that the \texttt{gyroADC} signal has already passed through the internal Betaflight filtering stages, whereas the \texttt{gyroUnfilt} signal represents the raw data directly measured by the gyroscope sensor without any filtering applied, as illustrated in Figure~\ref{fig:gyro_filtered_vs_unfiltered}.

\begin{figure}[h]
	\centering
	\includegraphics[width=0.7\linewidth]{Images/Theory/Control_Gyro.png}
	\caption{Filtered versus unfiltered gyroscope signal}
	\label{fig:gyro_filtered_vs_unfiltered}
\end{figure}

Transforming both signals into the frequency domain reveals the effect of the internal filters on the signal content. The filtered signal shows reduced high-frequency noise and attenuated frequency components, while the unfiltered signal contains stronger high-frequency noise and structural resonances.\parencite{bf_Spectra}

The spectrogram extends this analysis by showing how the frequency content of the gyroscope signal changes with throttle. The signal is divided into overlapping segments, transformed using an FFT, and averaged within discrete throttle bins \parencite{bf_Spectogram}.

This representation helps to understand how resonances, noise levels, and dominant frequency components change with throttle, giving insight into the system dynamics under different flight conditions.


\subsubsection*{Estimation of Frequency Response Including Rotational Filtering}

To estimate the frequency response of a system from measured input and output signals, the data must first be transformed into the frequency domain. This is achieved using the Fast Fourier Transform (FFT). The frequency response is then obtained by dividing the Fourier transform of the output signal by that of the input signal:

\begin{equation}
	G_{xy}(\omega) = \frac{\mathrm{FFT}(y)}{\mathrm{FFT}(x)} .
\end{equation}

In practical applications, measurement data is often contaminated by noise, which can significantly distort the frequency response estimation. As illustrated in Figure \ref{fig:system_noise}, noise is added to the system between the input signal \(u(t)\) and the output signal \(y(t)\). This noise may originate from sensor inaccuracies, environmental disturbances, or electronic interference. If not properly handled, it can lead to unreliable or misleading results. To mitigate this effect, the program applies a dedicated filtering routine, implemented in the function \texttt{apply\_rotfiltfilt}.

\begin{figure}[h]
	\centering
	\includegraphics[width=0.55\textwidth]{Images/Theory/Freq_Blockschalt.png}
	\caption{System model including measurement noise}
	\label{fig:system_noise}
\end{figure}

To improve the robustness of the frequency response estimation, a rotational filtering approach is used. In this method, the output signal is shifted to the baseband, filtered, and then shifted back to its original frequency range. The frequency shift is achieved by multiplying the signal with a complex exponential corresponding to the instantaneous chirp frequency:

\begin{equation}
	y_{\mathrm{rot}}(t) = y(t) \cdot e^{-j 2 \pi f_{\mathrm{chirp}}(t) t} .
\end{equation}

After shifting the signal to baseband, a zero-phase filter is applied to remove unwanted frequency components while preserving the phase information of the signal. This step is essential for reducing noise without distorting the underlying system dynamics. The filtered signal is then shifted back to its original frequency range \parencite{bf_ApplyRot}.

Subsequently, the frequency response is estimated using the filtered output signal within the function \texttt{Estimate Frequency Response}. The signal is divided into overlapping segments, as illustrated in Figure \ref{fig:segmentation}. The FFT is computed for each segment, and the resulting spectra are averaged to obtain a smooth and reliable estimate of the frequency response \parencite{bf_estfreqresp}.

\begin{figure}[h]
	\centering
	\includegraphics[width=0.9\textwidth]{Images/Theory/segmentation.png}
	\caption{Segmentation of a signal into overlapping windows}
	\label{fig:segmentation}
\end{figure}

\newpage

\subsubsection*{Transfer Function Estimation}

In a closed-loop control system, the plant is the physical system that we want to control, for example, the movement of a drone.
To tune a controller, we need to understand how the plant behaves. However, when the system is running in closed-loop, this becomes difficult.
The output is fed back through the controller and continuously changes the input signal. Because of this, the input is no longer an independent excitation but instead already contains the controller reactions and disturbance effects.

If we try to measure the transfer function directly from input to output in this situation, the result will not represent the true behavior of the plant.
Instead, it will show a combination of the plant dynamics, the controller dynamics, and the feedback loop.
Figure~\ref{fig:closedloop} shows a simplified closed-loop structure.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.65\textwidth]{Images/Theory/Control_Loop_easy.png}
	\caption{Simplified closed loop system}
	\label{fig:closedloop}
\end{figure}

To overcome this problem, we use the known relationships between the different transfer functions in the closed-loop.
From the measurement data we compute

\begin{equation}
	G_{yr}(\omega) = \frac{\mathrm{FFT}(y)}{\mathrm{FFT}(r)}
	= \frac{C(\omega)\,P(\omega)}{1 + C(\omega)\,P(\omega)},
\end{equation}

\begin{equation}
	G_{ur}(\omega) = \frac{\mathrm{FFT}(u)}{\mathrm{FFT}(r)}
	= \frac{C(\omega)}{1 + C(\omega)\,P(\omega)}.
\end{equation}

By dividing the two expressions, the controller cancels out and we obtain an estimate of the plant transfer function:

\begin{equation}
	P(\omega) = \frac{G_{yr}(\omega)}{G_{ur}(\omega)}.
\end{equation}

This allows us to reconstruct the plant transfer function \(P(\omega)\) from closed-loop measurement data without opening the control loop during the experiment \parencite{bf_Transfun}.


\subsubsection*{Closed-Loop Calculation}

After determining the plant transfer function, the next step is to analyze the behavior of the overall system when a controller is applied. This is achieved by evaluating the closed-loop transfer functions, which describe the system response, disturbance sensitivity, disturbance rejection capability, and the required controller effort. These measures provide valuable insights into the stability, robustness, and performance of the controlled system.

As an example, the closed-loop configuration shown in Figure \ref{fig:closed_loop} is analyzed.

\begin{figure}[h]
	\centering
	\includegraphics[width=0.65\textwidth]{Images/Theory/Control_Loop_easy.png}
	\caption{Simplified closed loop system}
	\label{fig:closed_loop}
\end{figure}

The closed-loop transfer function from the reference input to the output is given by
\begin{equation}
	G_{yr}(\omega) = \frac{C(\omega) P(\omega)}{1 + C(\omega) P(\omega)} .
\end{equation}

The sensitivity function, which describes how sensitive the system output is to disturbances and model uncertainties, is defined as
\begin{equation}
	S(\omega) = \frac{1}{1 + C(\omega) P(\omega)} .
\end{equation}

The compliance function, representing the system response to disturbances acting on the plant, is given by
\begin{equation}
	CC(\omega) = P(\omega) \cdot S(\omega) .
\end{equation}

Finally, the controller effort describes the influence of the reference input on the controller output and is defined as \parencite{bf_closeloop}
\begin{equation}
	SC(\omega) = C(\omega) \cdot S(\omega) .
\end{equation}


\newpage

\subsubsection*{Compensation of the I-Term}
The integral term of a PID controller is used to compensate for steady-state errors by integrating the control error over time. In flight control systems, this helps to counteract constant disturbances such as motor imbalances or aerodynamic effects. During fast stick movements or aggressive maneuvers, however, the integral term can accumulate too much error.

In these situations, the accumulated I-term no longer represents a disturbance but instead opposes the intended motion. When the maneuver ends, this can lead to overshoot, bounce-back effects, or slow settling of the system.

To reduce these effects, Betaflight uses a feature called I-Term Relax. This mechanism temporarily limits the integration of the I-term when rapid changes are detected. Depending on the configuration, these changes are identified either from the control setpoint or from the measured gyroscope signal. When the detected change exceeds a defined threshold, the I-term integration is reduced or paused.

This allows the controller to respond quickly to fast pilot inputs while preventing excessive buildup of the integral term. During slower movements and steady operation, normal integration is restored. As a result, I-Term Relax improves flight behavior by reducing overshoot and bounce-back while maintaining good disturbance rejection.

For analysis and controller tuning, the effect of I-Term Relax must be considered, as it modifies the controller behavior depending on the current flight conditions.
\begin{figure}[h]
	\centering
	\includegraphics[width=0.7\textwidth]{Images/Theory/Control_Loop_Iterm.png}
	\caption{Segmentation of a signal into overlapping windows}
	\label{fig:I_term}
\end{figure}

The measured controller transfer function is compared with an analytically derived controller transfer function. The effect of the I-Term Relax algorithm is therefore included implicitly in the measurement-based result.

To this end, a compensation factor is defined as the ratio between the measured PI controller transfer function and its analytical counterpart:
\begin{equation}
	C_{\mathrm{PI,com}}(\omega) = \frac{C_{\mathrm{PI}}(\omega)}{C_{\mathrm{PI,ana}}(\omega)} .
\end{equation}

This frequency-dependent compensation factor represents the combined influence of effects that are not explicitly modeled, including the impact of I-Term Relax. The factor is subsequently applied to both the original analytical PI controller and the newly designed PI controller:
\begin{align}
	C_{\mathrm{PI,ana,new}}(\omega) & = C_{\mathrm{PI,ana}}(\omega) \cdot C_{\mathrm{PI,com}}(\omega) , \\
	C_{\mathrm{PI,new,new}}(\omega) & = C_{\mathrm{PI,new}}(\omega) \cdot C_{\mathrm{PI,com}}(\omega) .
\end{align}

The resulting compensated controller transfer functions are then reinserted into the closed-loop system for further analysis and evaluation \parencite{bf_compITerm}.

\subsubsection*{Step Response}
The step response provides a direct view of how the system reacts to sudden changes in the input signal. Unlike the transfer function, which describes the system behavior in the frequency domain, the step response is defined in the time domain and therefore offers intuitive insight into the transient behavior of the system.

To obtain the step response, the closed-loop transfer function must be transformed back into the time domain. This is achieved by applying the inverse Fourier transformation to the output spectrum
\begin{equation}
	y(t) = \mathcal{F}^{-1}\{Y(\omega)\}
	= \mathcal{F}^{-1}\{G_{yr}(\omega)\cdot R(\omega)\}.
\end{equation}

Using the convolution theorem, this expression can be interpreted in the time domain as a convolution of the system's impulse response $g_{yr}(t)$ with the input signal $r(t)$:
\begin{equation}
	y(t) = (g_{yr} * r)(t)
	= \int_{0}^{t} g_{yr}(\tau)\, r(t-\tau)\, d\tau.
\end{equation}

For a step input, this convolution illustrates how the system accumulates the effect of its impulse response over time, leading to the characteristic shape of the step response \parencite{bf_stepresp}.

\newpage

\section{Challenges and Methodological Approach}

\subsection{Simulations}
At the beginning of our thesis, we developed several simulations to deepen our understanding of the existing code and the methods applied within it. Although creating these simulations was time-intensive, they proved essential for our work. They provided the foundation for all subsequent analysis steps and enabled us to fully understand the existing functions before applying them further.

\subsubsection*{Simulation of Betaflight Controller Order}
To gain a deeper understanding of the Betaflight control architecture, the complete control structure was reconstructed in MATLAB. The system’s step
response was simulated using the same PID parameters and filter settings as onthe actual drone.\par

The filter structure implemented in Betaflight was reproduced, with both continuous- and discrete-time versions created to allow a direct comparison of
their behavior, as shown in Fig.~\ref{fig:dis_cont}.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.85\textwidth]{./Images/Simulations/Dicrete_continuous.png}
	\caption{Comparison of continuous and discrete filters}
	\label{fig:dis_cont}
\end{figure}

Next, we implemented the PI and D controllers following the same principles. For the plant model, we used a simplified structure consisting of PT1 elements and a dead time, resulting in a transfer function that approximates the real system dynamics. The resulting simplified plant is shown in Figure \ref{fig:dis_plant_model}.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.85\textwidth]{./Images/Simulations/plant_meas_sim.png}
	\caption{Simplified plant model}
	\label{fig:dis_plant_model}
\end{figure}

All components were then integrated into a complete Simulink model representing the Betaflight gyro control loop, as shown in Figure~\ref{fig:simulinksimulation}. Using this setup, we simulated the system's step response. An additional objective of the simulation was to identify which signals in the Blackbox log correspond to the various states within the control loop.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.95\textwidth]{./Images/Simulations/Simulink.png}
	\caption{Simulink model of the Betaflight control system}
	\label{fig:simulinksimulation}
\end{figure}

The simulated step response, shown in Figure~\ref{fig:step_resp_sim}, closely matched the response of the real drone. This result confirmed that our implementation captured both the structure and the dynamic behavior of the Betaflight control system sufficiently well. Working through this simulation allowed us to check whether we had understood the control loop correctly and gave us a reliable starting point for the later calculations.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.95\textwidth]{./Images/Simulations/step_resp_simulink.png}
	\caption{Simulated step response}
	\label{fig:step_resp_sim}
\end{figure}


\newpage
\subsubsection*{Simulation PID Controller}
To get a better understanding of the individual controller components, we wrote an additional MATLAB script that lets us combine different controller types with simple plant models. The user can switch between P, I, PI, PD, and PID controllers, as well as between a few basic plant types. This makes it easier to see how each controller part affects the overall system behavior. The script calculates the open-loop and closed-loop transfer functions and produces Bode plots, Nyquist diagrams, and step responses. Figure~\ref{fig:controllerbode} shows an example Bode plot from this simulation.

\begin{figure}[htbp]
	\centering
	\includegraphics[width=1.0\textwidth]{./Images/Simulations/Controller_Bode.png}
	\caption{Bode plot from Controller Simulation}
	\label{fig:controllerbode}
\end{figure}

The frequency response of the shown PID controller is based on the parameters
$K_p = 100$, $K_i = 10$ and $K_d = 10$.
The proportional gain defines the magnitude level in the mid-frequency range:
\begin{equation}
	20\log_{10}(K_p) = 20\log_{10}(100) = 40\,\text{dB}.
\end{equation}

The integral term causes the slope of $-20\,\text{dB/decade}$ at low frequencies with
\begin{equation}
	|C_I(j\omega)| = \frac{K_i}{\omega} = \frac{10}{\omega}.
\end{equation}

The corresponding lower corner frequency is
\begin{equation}
	\omega_{k,I} = \frac{K_i}{K_p} = \frac{10}{100} = 0.1\,\text{rad/s}.
\end{equation}

The derivative term causes the slope of $+20\,\text{dB/decade}$ at high frequencies with
\begin{equation}
	|C_D(j\omega)| = K_d\,\omega = 10\,\omega.
\end{equation}

The corresponding upper corner frequency is
\begin{equation}
	\omega_{k,D} = \frac{K_p}{K_d} = \frac{100}{10} = 10\,\text{rad/s}.
\end{equation}

This example also shows that changing the integral and derivative gains shifts the position of the slopes in the Bode plot. A larger integral gain moves the low-frequency slope towards higher frequencies, while a larger derivative gain moves the high-frequency slope towards lower frequencies. In this way, the shape of the frequency response can be adjusted to the desired system behavior.

\subsubsection*{Simulation Frequency Response}

To analyze the frequency response of a system using input and output data, we created an additional simulation. In this setup, a chirp signal was used as the input signal. As in a real system, we added noise to both the input and the output to mimic realistic measurement conditions. From these noisy signals, we attempted to estimate the original system transfer function. The corresponding control loop is shown in Figure~\ref{fig:Freq_loop}.

\begin{figure}[htbp]
	\centering
	\includegraphics[width=0.6\textwidth]{./Images/Simulations/Freq_Blockschalt.png}
	\caption{Control loop of the simulation}
	\label{fig:Freq_loop}
\end{figure}

As in the real system, we could not measure the plant without noise. Instead, we had to measure $u(t)$ and $y(t)$ and estimate the transfer function from these signals, and not $\tilde{u}(t)$ and $\tilde{y}(t)$, which would allow us to calculate the transfer function directly. The different signals are shown in Figure \ref{fig:Freq_input}.

\begin{figure}[htbp]
	\centering
	\includegraphics[width=0.9\textwidth]{./Images/Simulations/Freq_Input_Output.png}
	\caption{Input and output signals with noise}
	\label{fig:Freq_input}
\end{figure}

If we calculate the transfer function directly from the noisy signals using a plain FFT, we obtain a very poor estimate, as shown in Figure~\ref{fig:Freq_FFT}. The noise distorts the frequency response significantly.

\begin{figure}[htbp]
	\centering
	\includegraphics[width=0.95\textwidth]{./Images/Simulations/Freq_Y_U.png}
	\caption{Bode plot estimated with plain FFT}
	\label{fig:Freq_FFT}
\end{figure}

To improve this poor estimate, we applied different signal-processing methods. First, we used the Welch method, which improves the estimation by averaging over overlapping segments. The implementation of the Welch frequency response function (FRF) follows the theory described in the documentation \href{https://github.com/InsaneBroccoli/bf_controller_tuning/blob/PA_final/markdown/Frequency_Response/estimate_frequency_response.md}{Estimate
	Frequency Response}. This allowed us to compare the plain FFT approach with the more robust Welch estimator. The result is shown in Figure~\ref{fig:Freq_Welch}.

\begin{figure}[htbp]
	\centering
	\includegraphics[width=0.95\textwidth]{./Images/Simulations/Freq_Welch.png}
	\caption{Bode plot estimated with Welch method}
	\label{fig:Freq_Welch}
\end{figure}

In the next step, we also implemented a rotated-signal approach, where the chirp is demodulated to baseband before filtering. For this part, we used the zero-phase filtering method \texttt{apply\_rotfiltfilt}, as described in the documentation \href{https://github.com/InsaneBroccoli/bf_controller_tuning/blob/PA_final/markdown/Frequency_Response/ApplyRotfiltfilt.md}{Apply Rotfiltfilt}. Finally, we compared the original Bode plot to the signal-processed one, as shown in Figure~\ref{fig:Freq_Rot}, to assess how the rotation technique improves noise robustness.

\begin{figure}[htbp]
	\centering
	\includegraphics[width=0.95\textwidth]{./Images/Simulations/Freq_Rot.png}
	\caption{Bode plot estimated with rotated Welch method}
	\label{fig:Freq_Rot}
\end{figure}

\newpage


\subsubsection*{Simulation Spectra}
To better understand the components required to generate a meaningful spectrum, we developed a simulation based on the system used in the provided code. In this script, we generated a noisy sinusoidal input signal and computed different spectra from it. In one case, a window function was applied, while in the other case no windowing was used, as illustrated in Figure~\ref{fig:spectra_input}.

\begin{figure}[htbp]
	\centering
	\includegraphics[width=0.85\textwidth]{./Images/Simulations/Spectra_input.png}
	\caption{Spectrum with and without windowing}
	\label{fig:spectra_input}
\end{figure}

In the next step, we compared the frequency spectrum of the signal with and without the window. As shown in Figure~\ref{fig:spectra_window}, the spectrum without windowing exhibits strong spectral leakage, which appears as energy spreading around the main frequency peak and as several side peaks that distort the actual frequency content. In contrast, the spectrum with the Hann window applied shows much less leakage, resulting in a clearer and more accurate representation of the signal's frequency components.

\begin{figure}[htbp]
	\centering
	\includegraphics[width=0.9\textwidth]{./Images/Simulations/Spectra_window.png}
	\caption{Comparison of spectra with and without windowing}
	\label{fig:spectra_window}
\end{figure}

In the last step, we split the signal into several overlapping segments, as described in the documentation \href{https://github.com/InsaneBroccoli/bf_controller_tuning/blob/PA_final/markdown/Spectrum_Spectogram/Spectra_Analysis.md}{Spectra Analysis}. By averaging across these segments, we were able to observe how this method reduces noise and stabilizes the resulting spectrum.

\begin{figure}[htbp]
	\centering
	\includegraphics[width=0.9\textwidth]{./Images/Simulations/Spectra_segment.png}
	\caption{Spectrum with segment averaging}
	\label{fig:spectra_seg}
\end{figure}

\newpage

\subsubsection*{Simulation Spectrogram}

To analyze how the frequency content of an input signal changes with both time and thrust, we developed a MATLAB simulation. In this script, we generated a noisy sinusoidal input signal with a slowly varying instantaneous frequency, together with a slowly varying thrust signal. The thrust is modeled as a sinusoid around a mean value of 2. To illustrate the effect of windowing, a Hann window was also applied to the full input signal, as shown in Figure~\ref{fig:spectogram_input}.

\begin{figure}[htbp]
	\centering
	\includegraphics[width=0.8\textwidth]{./Images/Simulations/Spectogram_overview.png}
	\caption{Input signal, windowed input signal, and thrust over time}
	\label{fig:spectogram_input}
\end{figure}

In the next step, the input signal was divided into short, non-overlapping time windows. For each segment, we computed the discrete Fourier transform (FFT) to obtain a local frequency spectrum. By arranging these spectra along the time axis, we created a time-frequency representation of the signal, as shown in Figure~\ref{fig:spectogram_time}.

\begin{figure}[htbp]
	\centering
	\includegraphics[width=0.8\textwidth]{./Images/Simulations/Spectogram_Time_Freq.png}
	\caption{Spectrogram showing the amplitude over frequency and time}
	\label{fig:spectogram_time}
\end{figure}

To relate the spectral content to the thrust level, we assigned each segment the mean thrust value within its time interval. The corresponding spectra were then plotted over frequency and mean thrust, resulting in a spectrogram that shows how the frequency content varies with thrust for this example, as shown in Figure~\ref{fig:spectogram_thrust}.

\begin{figure}[htbp]
	\centering
	\includegraphics[width=0.8\textwidth]{./Images/Simulations/Spectogram_Thrust_Freq.png}
	\caption{Spectrogram showing the amplitude over frequency and thrust}
	\label{fig:spectogram_thrust}
\end{figure}

\newpage

\subsection{Building an FPV drone}
\label{subsec:BuildingFPVdrone}
Besides understanding the code and further developing the proof of concept,
building a drone and setting it up correctly bridges the gap between the digital domain,
where algorithms and simulations are developed, and the physical reality of drone operation.
By working with the hardware, it is easier for us to understand what practical challenges exist for pilots and the community.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.6\textwidth]{./Images/OvershootExpress.jpg}
	\caption{aosmini 3-inch build}
	\label{fig:OvershootExpress}
\end{figure}

Therefore, a three-inch FPV drone was built from the ground up.
This work included:
\begin{itemize}
	\item creating schemes to see which connections were required
	\item soldering all the components such as the motors, receiver, video system, logger, and GPS module to the flight controller
	\item attaching everything to the frame so that nothing generates unnecessary oscillations during flight
	\item double-checking all soldering connections and screws
	\item setting everything up in Betaflight
	\item test-flying the drone
	\item drone tuning
\end{itemize}

The flight controller barely fit into this frame. The GPS mount had to be trimmed off a little bit so that we could mount the all-in-one stack. The video system was also mounted very close to the stack. Due to a connector between these two parts, we had to lift the video system up with some spacers. This resulted in insufficient space on top of the video system for the screws, which attach the component to the frame. The video system then had to be mounted with just two screws instead of four. In the first test flights, no unusual vibrations were observed, so it's probably mounted adequately. As we need to be able to get the microSD card out of the logger, the small PCB has to be mounted somewhere outside the drone. We designed a small 3D-printed surface where the logger could be mounted with some double-sided tape. This component then was the lowest part of the drone, which was a problem. In one of the first flights, the microSD card holder opened and we lost the card during landing.

To prevent this from happening again, we need to place the logger somewhere else or design some stands for the drone. This is being addressed in a later step, as it is not fundamental for the tuning process.

\subsection{Drone tuning}
As part of understanding the workflow, we tuned an FPV drone ourselves. Initially, we applied a poor tuning configuration to observe its effects. During the first field test with this setup, it quickly became clear that the tuning was severely inadequate. The drone began oscillating aggressively and climbed rapidly. As the drone was out of control, the kill switch had to be activated, resulting in a crash that broke one of the four arms. Fortunately, the damage was minor and repaired quickly.

After this incident, we decided not to repeat flights with intentionally poor tuning. Instead, we continued using the default Betaflight parameters as a baseline. With these, the drone flies quite well. However, during maneuvers such as flips or propwash maneuvers, it becomes evident that there is room for improvement in terms of stability and responsiveness.

The tuning process was carried out on a larger 6-inch FPV drone provided by one of the team members and on the 3-inch drone we built (See Section \ref{subsec:BuildingFPVdrone}). In this documentation, we focus on the tuning process for the 3-inch build.
To enable the chirp flight mode, the flight controller was freshly flashed with Betaflight version 2025.12.0-RC1, meaning all filters and settings were at factory defaults. Before the first flight, we already enabled the RPM filter and changed the motor poles from fourteen to twelve because this filter is much more effective at filtering out motor noise.

\subsubsection*{The tuning process using our tool}

Our tool generates more plots than essentially needed for tuning. While some provide useful diagnostic information, they are not directly used in the tuning process itself. Figure~\ref{fig:evaluationTime_tuning} shows how stable and consistent the flight controller loop time was during flight. Figure~\ref{fig:flightoverview_tuning} gives a broad summary of gyro rates, motor outputs, and throttle. This plot is important for verifying that the motor outputs don't drop to minimum throttle during the chirp measurement. This would interrupt the system response and invalidate the frequency data. Figure~\ref{fig:flightgyrodata_tuning} shows how well the drone tracks commanded inputs on each axis, providing a visual overview of the setpoint tracking performance.

\begin{figure}[H]
	\centering
	\begin{minipage}{0.32\textwidth}
		\centering
		\includegraphics[width=\textwidth]{./Images/Tuning_OvershootExpress/evaluation_time.jpg}
		\caption{Flight controller loop time}
		\label{fig:evaluationTime_tuning}
	\end{minipage}
	\hfill
	\begin{minipage}{0.32\textwidth}
		\centering
		\includegraphics[width=\textwidth]{./Images/Tuning_OvershootExpress/flight_overview.jpg}
		\caption{Flight overview}
		\label{fig:flightoverview_tuning}
	\end{minipage}
	\hfill
	\begin{minipage}{0.32\textwidth}
		\centering
		\includegraphics[width=\textwidth]{./Images/Tuning_OvershootExpress/flight_gyro_data.jpg}
		\caption{Gyro setpoint tracking}
		\label{fig:flightgyrodata_tuning}
	\end{minipage}
\end{figure}

\subsubsection*{Filter Tuning}
Before we start tuning the PID controllers, we need good filtering to have clean signals to work with. The general aim here is to use as little filtering as possible because too much filtering causes delay in the system. However, no quadcopter build is perfect, so at least some filtering is necessary in order to achieve a well-tuned system.

To do this, we started looking at the spectrograms and the spectra figures first after importing a log file into MATLAB. After running the program in MATLAB, you can see in the command window which filter and PID settings were used in this log file. Our tool, in its current state, doesn't copy these values to the code automatically. So the first step was to copy the settings from the command window to the correct position in the code. By then comparing the filtered and unfiltered plots, you can see how well the current settings filter out unwanted vibrations. Note that dynamic filters can't be calculated offline because their cutoff frequencies change continuously based on real-time throttle input, so changing the dynamic filter settings in the code will not show up in the plots. In order to see the changes, you need another flight.

The \textbf{spectra} in Figure~\ref{fig:Spectra_tuning} shows the frequency composition of the gyro signals.
Peaks in this plot point to vibration sources at specific frequencies.
When we analyze this plot, we look for three main things:
\begin{itemize}
	\item \textbf{Resonance Peaks:} If all three axes show a peak at the same frequency, it usually means there is a resonance in the frame or due to a poorly fixed component. This can be fixed with a specific notch filter.
	\item \textbf{Noise:} A high noise level indicates general vibration issues. Clean lines in the plot show that the filtering is working well.
	\item \textbf{Axis Sum:} The axis sum shows the overall vibration level across all gyro axes. A properly filtered signal drops quickly after the motor frequencies (after around 600 Hz) and remains low, indicating effective suppression of high-frequency noise and resonances.
\end{itemize}

\begin{figure}[H]
	\centering
	\includegraphics[width=\textwidth]{./Images/Tuning_OvershootExpress/Spectra.jpg}
	\caption{Spectra}
	\label{fig:Spectra_tuning}
\end{figure}

We can see in this figure that our filter settings work well. The filtered signals have very little noise and no large resonances. Only the yaw axis has a small peak around 550 Hz, which is acceptable. The axis sum drops nicely and stays low after about 600 Hz, right where the motor frequencies end. This shows the drone is running smoothly with vibrations under control.

The vibrations are also visible in the \textbf{spectrogram} plots in Figure~\ref{fig:Spectrograms_tuning}.
Here, frequencies are plotted against thrust level and the color shows the signal intensity. Red equals high energy and blue low energy.

\begin{figure}[H]
	\centering
	\includegraphics[width=\textwidth]{./Images/Tuning_OvershootExpress/Spectrograms.jpg}
	\caption{Spectrograms}
	\label{fig:Spectrograms_tuning}
\end{figure}

\textbf{Vertical lines} in the spectrogram indicate vibrations at fixed frequencies that do not change with thrust. These usually come from frame resonances or loose components. Such resonances can be removed with static notch filters, but because they are concentrated at a single frequency, they must be used carefully. For more robust suppression, especially when frequencies shift due to bent props or changing load conditions, a dynamic notch filter is usually the better choice.

\textbf{Diagonal lines} show vibrations that change with motor speed, such as propeller noise. Using the RPM Filter, these propeller vibrations can be filtered out quite well. This filter uses real-time RPM telemetry data to place notch filters exactly on the fundamental and harmonic frequencies of each motor. As propeller vibrations are directly proportional to motor RPM, this approach eliminates noise at its source instead of relying on fixed or dynamic estimation. In our case, we kept the RPM filter with the default settings. We used three-bladed propellers, so setting the filter to three harmonics was sufficient, as we can see in Figure~\ref{fig:Spectrograms_tuning} under the filtered plots.

\textbf{Aliasing}\\
When you try to sample a signal that contains frequencies higher than half of your sampling rate, aliasing can occur. This would make high-frequency content appear as lower frequencies, corrupting the measurement data. To avoid this, we set a PT1 low-pass filter. The cutoff frequency should not be greater than the sampling frequency divided by two. As we use a 2 kHz logging frequency, we set the cutoff to 800 Hz as a conservative choice.
If aliasing occurs on your drone, it can be seen in the spectrogram as diagonal lines going down as you move to higher frequencies.

\textbf{Dynamic notch filter}\\
The dynamic notch filter automatically finds and removes mechanical motor noise with the highest amplitude for each axis. The standard setting here is 2 notches from 100 to 600 Hz. Through testing, we found that changing the notch filter count from two to three provided better filtering. We can see in Figure~\ref{fig:Spectrograms_tuning} that the filtered plots don't show any diagonal lines anymore.

\textbf{D-term low-pass filter}\\
The D-term in a PID controller amplifies high-frequency gyro noise. So this filter controls how much noise is fed back into the control loop. If too much noise passes through, the D-term causes rapid micro-oscillations in the motors, leading to overheating and potential hardware failure.
For our initial flight, we configured a PT3 (third-order) filter with a cutoff of 100 Hz. We chose this filter because PT3 has a much steeper slope than a standard first- or second-order filter. This allows us to aggressively suppress high-frequency noise while minimizing delay in the lower frequencies where the actual flight control happens. The 100 Hz cutoff served as a conservative starting point. In further tuning, this value can be increased to improve responsiveness while keeping the motors cool.

\textbf{Yaw low-pass filter}\\
The yaw low-pass filter smooths the gyroscope signal by ignoring high-frequency noise. Since the yaw axis typically experiences less mechanical vibration than roll and pitch, the filter can often be configured less aggressively without compromising signal quality.
For our setup, we retained the default Betaflight settings, as they provided good noise suppression without creating unnecessary delay.

Further information about spectra and spectrograms can be found \href{https://github.com/InsaneBroccoli/bf_controller_tuning/blob/PA_final/markdown/Spectrum_Spectogram}{here}. Also consider looking at the \href{https://www.betaflight.com/docs/wiki/configurator/pid-tuning-tab}{Betaflight wiki page} for more information about Betaflight filters.

\subsubsection*{PID tuning}
After configuring the filter settings, we could move on to the PID tuning.

The tool calculates the Plant and Controller Bode plots to build a mathematical model of the drone. These are interesting for an engineer but not necessarily useful for a user in the community, as they are less intuitive. We mainly focused on the Step Response and the Gang of Four, where you can see flight performance and stability directly.

\begin{figure}[H]
	\centering
	\begin{minipage}{0.48\textwidth}
		\centering
		\includegraphics[width=\textwidth]{./Images/Tuning_OvershootExpress/plant.jpg}
		\caption{Plant Bode plot}
		\label{fig:plant_tuning}
	\end{minipage}
	\hfill
	\begin{minipage}{0.48\textwidth}
		\centering
		\includegraphics[width=\textwidth]{./Images/Tuning_OvershootExpress/Bode_controller_pitch.jpg}
		\caption{Controller Bode plot}
		\label{fig:bodeController_tuning}
	\end{minipage}
\end{figure}

The \textbf{step response} plot shows exactly what happens when the pilot moves the stick rapidly. You can see how fast the drone reacts to the command, if it overshoots the target value, and how quickly it settles to a steady state.

\begin{figure}[H]
	\centering
	\includegraphics[width=1.0\textwidth]{./Images/Tuning_OvershootExpress/Step_response_pitch_tuned.jpg}
	\caption{Step Response}
	\label{fig:step_response_tuning}
\end{figure}

\begin{itemize}
	\item Tracking (Top Plot): It shows how the drone follows a command.
	\item Compliance (Bottom Plot): It shows how the drone reacts to external disturbances, like wind.
\end{itemize}

\textbf{Gang of Four}\\
The Gang of Four is a standard set of four transfer functions that together fully characterize a feedback control system. These four plots show the Sensitivity (S), Complementary Sensitivity (T, Bode plot), Controller Effort (SC), and Compliance (SP). By looking at these plots, we can see if the drone is stable, how well it follows commands, how it handles wind, and whether the motors are working too hard, all in one view.

During our tuning, we paid specific attention to three of these plots:
\begin{enumerate}
	\item Sensitivity (S):
	      It shows how the system responds to noise and reference commands. The peak value tells us how stable the system is. For a well-damped system, the peak should be below 6 dB because higher values mean the drone could start oscillating or become unstable.
	\item Controller Effort (SC):
	      Indicates how hard the controller is working. If the effort is too high at high frequencies, the system is reacting to sensor noise rather than real disturbances. This causes unnecessary motor activity, leading to heating and wear.
	\item Compliance (SP):
	      It shows how much external disturbances (like wind gusts) affect the drone's motion. A lower compliance is better; it means the drone resists disturbances more effectively.
\end{enumerate}

\begin{figure}[H]
	\centering
	\includegraphics[width=1.0\textwidth]{./Images/Tuning_OvershootExpress/gang_of_four_pitch_tuned.jpg}
	\caption{Gang of Four}
	\label{fig:gangoffour_tuning}
\end{figure}

\textbf{PID tuning summary}\\
Our tuning objective was to achieve a fast step response without overshoot while maintaining good disturbance rejection and stability margins. It is important to note that there is no universally perfect tune. Settings depend heavily on pilot preferences. Racing pilots typically prefer aggressive, responsive tuning for competitive performance, while freestyle pilots may prioritize smoothness and flight feel. Since none of our team members is a professional pilot, we focused on achieving robustness and a step response without overshoot. This tuning approach is probably somewhere between what a racing pilot would want and a freestyle pilot would prefer. Robustness is practical in this case because it handles scenarios such as slightly bent propellers, a battery positioned off-center, or additional components such as cameras mounted on the drone. It should also be mentioned that the axes roll and pitch should be tuned as similarly as possible. This is because symmetrical tuning makes the drone more intuitive to pilot. When both axes respond identically to stick inputs, the drone behaves predictably during combined maneuvers, making control feel natural.

The tuning process involved iteratively adjusting PID gains in MATLAB while monitoring both the step response and Gang of Four plots side by side. We found and used an online \href{https://www.luisllamas.es/en/pid-controller-simulator/}{PID simulator} as a guide to build intuition for how P, I, and D values affect system behavior. For the step response, we aimed for a fast rise time with minimal or no overshoot and a smooth curve without oscillations. On the Gang of Four side, we wanted the sensitivity peak to stay below 6 dB for good stability margins, low compliance across the control bandwidth for effective disturbance rejection, and reasonable controller effort to avoid amplifying sensor noise. All transfer functions should show smooth, rounded shapes without sharp resonances that indicate problematic dynamics. Once we were satisfied with the curves and their shapes, we could take the P, I, and D values for each axis from the command window in MATLAB and set them via the Betaflight configurator to the drone.

Achieving an optimal tune required several iterations due to challenges with filter adjustments and occasional logging issues. Nevertheless, the drone was successfully tuned, and the improvement was immediately noticeable. During propwash maneuvers, the drone remained significantly more stable, and flips were executed with greater precision at the stop point.


\textbf{Additional Resources}\\
For in-depth technical background on transfer functions and control theory, see our \href{https://github.com/InsaneBroccoli/bf_controller_tuning/tree/PA_final/markdown/Frequency_Response}{Frequency Response documentation}.
Detailed explanations of all generated plots and their interpretation can be found in the \href{https://github.com/InsaneBroccoli/bf_controller_tuning/tree/PA_final/markdown/Descriptions_Figures}{Figure Documentation}.


\newpage

\subsection{Decoding Betaflight Blackbox Logs}
\subsubsection*{Purpose}
Currently, to obtain a usable Blackbox log from Betaflight, pilots rely on the Betaflight Explorer.
This is a reliable tool but lacks convenience if the tuning tool is used iteratively since you have to visit the Explorer every time you want to download a new log.
To improve this workflow, we tried to make use of the existing open-source blackbox decode tool.

\subsubsection*{\href{https://github.com/betaflight/blackbox-tools/}{Blackbox Decode Tool}}
The blackbox decode tool is a C-based command-line utility designed to convert Betaflight Blackbox log files from their proprietary binary format into more accessible formats such as CSV.
It offers similar core functionality to the Betaflight Blackbox Explorer but is intended for users who prefer command-line tools or need to automate log processing tasks.\par

However, integrating blackbox decode into our project presented several challenges.
The most obvious issue is the absence of header information by default.
While this can be addressed by using the --save-headers flag, the format of these headers differs from that used by the Betaflight Explorer.
This discrepancy is manageable, as we could adapt our data import functions to handle both formats.\par

A more significant problem is the presence of numerical differences in the decoded data.
When comparing logs decoded with blackbox decode to those processed with Betaflight Explorer, the resulting datasets differ structurally and semantically in several critical ways.
Specifically, the blackbox decode stores the vehicle attitude as Euler angles in degrees, whereas the Explorer provides quaternion-like components.
With priority set on other tasks, we decided to set aside the integration of blackbox decode for now.

\newpage

\subsection{Code Structure and Programming Style}
From the start of the project, we recognized that a clear and maintainable code structure would be essential.
The initial proof-of-concept code already contained a rough separation into topics such as data I/O and step response,
but everything was packed into a single file, making it difficult to navigate.
Our first step was to comment out the proof-of-concept code and reorganize it into thematic sections.
This was the foundation for a more organized layout, and we arrived at an initial directory structure:
\\
\dirtree{%
	.1 bf\_controller\_tuning/.
	.2 main.m.
	.2 data\_io/.
	.3 load\_data.m.
	.3 extract\_header\_information.m.
	.3 preprocess\_data.m.
	.2 flight\_parameters/.
	.3 get\_pid\_parameters.m.
	.3 scale\_pid\_parameters.m.
	.3 print\_parameters.m.
	.2 spectrogram/.
	.3 calculate\_spectrogram.m.
	.3 plot\_spectrogram.m.
	.3 show\_spectrogram.m.
	.2 spectra/.
	.3 estimate\_spectra.m.
	.3 plot\_spectra.m.
	.3 show\_spectra.m.
	.2 frequency\_response/.
	.3 estimate\_frequency\_response.m.
	.3 plot\_bode.m.
	.3 show\_frequency\_response.m.
	.2 controller\_analysis/.
	.3 calculate\_closed\_loop.m.
	.3 calculate\_step\_response.m.
	.3 show\_controller\_analysis.m.
	.2 utils/.
	.3 apply\_rotfiltfilt.m.
	.3 downsample\_frd.m.
	.3 get\_my\_colors.m.
}

This draft was never intended to be final. Rather, it served as an initial attempt to bring clarity and separation between different functional areas.

\newpage

\subsubsection*{OOP vs. Functional Approach}
At this stage, we discussed whether to implement the entire project using object-oriented programming (OOP) or stick to a modular,
function-based approach. Initially, we considered OOP as a challenge since most of us were not very experienced with it.
We started writing some components as classes but quickly realized that only one team member was really comfortable with OOP,
while the others struggled. This made us reconsider.
Our conclusion was to use OOP selectively—only for parts that benefit from reuse and encapsulation, such as data loading and plotting,
while keeping the rest function-based for simplicity. This hybrid approach allowed us to maintain flexibility without overcomplicating the design.

After several attempts, we agreed on the following principles:

\begin{itemize}
	\item A main script (main.m) for user input, figure selection, filter settings, and PID tuning
	\item A core class to handle all calculations (essentially the logic from the proof of concept, but without plotting)
	\item A plot utilities class to centralize all plotting functions for consistency
\end{itemize}

Existing library functions from the proof of concept were reused without modification. The thematic grouping included:
\\
\dirtree{%
	.1 bf\_controller\_tuning/.
	.2 class/.
	.3 main\_class.m.
	.3 plot\_utils.m.
	.2 lib/.
	.3 apply\_rotfiltfilt.m.
	.3 calculate\_closed\_loop.m.
	.3 calculate\_controllers.m.
	.3 calculate\_step\_response\_from\_frd.m.
	.3 calculate\_transfer\_functions.m.
	.3 downsample\_frd.m.
	.3 estimate\_frequency\_response.m.
	.3 estimate\_spectra.m.
	.3 estimate\_spectrogram.m.
	.3 expand\_multiple\_figure\_nr.m.
	.3 extract\_header\_information.m.
	.3 get\_chirp\_signals.m.
	.3 get\_fcut\_from\_D\_and\_fcenter.m.
	.3 get\_fcut\_from\_exp.m.
	.3 get\_filter.m.
	.3 get\_ind\_eval.m.
	.3 get\_my\_colors.m.
	.3 get\_notch\_Q.m.
	.3 get\_pid\_scale.m.
	.3 get\_switch\_case\_text\_from\_para.m.
	.2 logs/.
	.2 main.m.
}

This structure worked well and kept the main script simple for the user.
However, after a review meeting, our instructor suggested further modularization.
The reason: we had created a "god class," which limited flexibility. For example,
if someone wanted to use the tool only for plotting spectrograms without any drone-specific logic, the current design made that difficult.
The program was also slow. All calculations were performed every time the user changed something.
We wanted to improve the speed by only recalculating what was relevant for each specific user input change.

\subsubsection*{Final Structure Concept}
Based on this feedback, we decided to split this god class into three classes.
The \texttt{flight\_analyzer} class handles frequency-domain analysis and vibration characterization.
Its core responsibility is performing spectral analysis on flight log data to identify resonance frequencies and assess filter effectiveness.
It computes the power spectral density of gyro signals (both filtered and unfiltered) and generates spectrograms showing
how vibration frequencies vary with throttle levels across all three axes (roll, pitch, yaw).
This analysis is essential for identifying noise sources, evaluating filtering strategies, and optimizing drone stability during tuning.
The \texttt{flight\_data} class is responsible for loading, parsing, and managing flight log data.
It provides methods to extract relevant information from Blackbox logs, such as gyro and motor data, and prepares this data for analysis.
The \texttt{gyro\_ctrl\_tuning} class focuses on tuning the PID controllers for the drone's flight.
It uses the results from the frequency response and spectral analysis to adjust the PID parameters, aiming to optimize the drone's flight characteristics.
The \texttt{plot\_utils} class remains responsible for visualization and graphical representation of all analysis results.
Unlike the previous approach, this class is now more flexible and reusable. It can be used independently to generate plots from pre-calculated data,
making it suitable for users who only want to visualize spectrograms, frequency responses, or step responses without running the full analysis pipeline.
This modularity allows the plotting functionality to be easily adapted for different visualization needs and integrated into other workflows.

This approach maintains thematic organization but makes it easier to use only specific parts of the tool.

\dirtree{%
	.1 bf\_controller\_tuning/.
	.2 class/.
	.3 flight\_analyzer.m.
	.3 flight\_data.m.
	.3 gyro\_ctrl\_tuning.m.
	.3 plot\_utils.m.
	.2 lib/.
	.3 apply\_rotfiltfilt.m.
	.3 calculate\_closed\_loop.m.
	.3 calculate\_controllers.m.
	.3 calculate\_step\_response\_from\_frd.m.
	.3 calculate\_transfer\_functions.m.
	.3 downsample\_frd.m.
	.3 estimate\_frequency\_response.m.
	.3 estimate\_spectra.m.
	.3 estimate\_spectrogram.m.
	.3 expand\_multiple\_figure\_nr.m.
	.3 extract\_header\_information.m.
	.3 get\_chirp\_signals.m.
	.3 get\_fcut\_from\_D\_and\_fcenter.m.
	.3 get\_fcut\_from\_exp.m.
	.3 get\_filter.m.
	.3 get\_ind\_eval.m.
	.3 get\_my\_colors.m.
	.3 get\_notch\_Q.m.
	.3 get\_pid\_scale.m.
	.3 get\_switch\_case\_text\_from\_para.m.
	.2 logs/.
	.2 main.m.
}

\newpage

\subsection{Conversion from MATLAB to Python}

To make the project more accessible to the FPV community, we decided to migrate the existing MATLAB codebase to Python.
The first step was to select suitable Python libraries that offer similar functionality to MATLAB while maintaining code efficiency and readability.
After evaluating several options, we chose the following libraries:
\begin{itemize}
	\item \textbf{NumPy}: For numerical operations and array manipulations, analogous to MATLAB's matrix operations.
	\item \textbf{SciPy}: For scientific computing tasks, including signal processing functions comparable to MATLAB's capabilities.
	\item \textbf{Matplotlib}: For plotting and visualization, providing features similar to MATLAB's plotting tools.
	\item \textbf{Pandas}: For data manipulation and analysis, useful for handling tabular data as in MATLAB tables.
	\item \textbf{Control Systems Library (control)}: For control system analysis and design, offering functions equivalent to MATLAB's Control System Toolbox.
\end{itemize}
We selected Python version 3.12, as it is well-supported and compatible with the required libraries.
To validate these choices, we developed a prototype script that implements the core functionalities needed for the Drone Tuning Tool.
The script loads flight log data from CSV files, parses and caches the data efficiently, and extracts relevant signals such as setpoints and gyro measurements.
NumPy and Pandas are used for numerical and tabular data handling, while SciPy provides signal processing utilities for windowing and frequency analysis.
The Control Systems Library is employed to estimate the system's frequency response and step response from flight data, replicating MATLAB's identification routines.
Matplotlib is used for visualizing raw signals, chirp responses, and system characteristics.
The modular structure, including custom data classes and caching, ensures fast and reproducible analysis.
This approach demonstrates that Python and its scientific libraries are suitable replacements for MATLAB in the drone tuning workflow.
While Pandas is not strictly necessary for the prototype, it simplifies data handling and improves code readability.
However, to minimize dependencies and ensure cross-platform compatibility, we may consider removing Pandas in the final implementation.
The flexibility of Python also allows for easier integration with other tools and automation of data processing tasks, which is beneficial for large-scale log analysis.

\subsection{Python in MATLAB}

In our initial porting attempt, we utilized MATLAB's built-in functionality to execute Python code directly from within the MATLAB environment.
While this approach allowed us to access Python libraries and scripts, it introduced some challenges.
One major issue was the frequent need for unit conversions between MATLAB and Python data types, which often led to subtle bugs and inconsistencies.
Additionally, we encountered differences in the implementation of certain algorithms, such as varying scaling conventions in the state-space representation of transfer functions.
These discrepancies complicated the integration process and made it difficult to ensure consistent results across both platforms.
Managing library dependencies and Python environments within MATLAB also proved cumbersome, especially when working across different operating systems or MATLAB versions.
As a result, this method proved unreliable for our project's requirements.
We ultimately decided to pursue a native Python implementation to avoid these integration issues and streamline the development workflow.

\subsection{Transferring the MATLAB Library to Python}

The core functionality of the Drone Tuning Tool resides in its library functions.
To ensure that the Python implementation is both reliable and consistent with the original MATLAB code, we systematically transferred each library function from MATLAB to Python.
For functions with simple numerical outputs, we used \texttt{np.testing.assert\_allclose} with appropriate absolute and relative tolerances, saving the function inputs and outputs from MATLAB and comparing them with the Python implementation.
For more complex functions, such as those involving state-space outputs, we developed dedicated test scripts to compare the behavior of the MATLAB and Python versions across a range of scenarios.

The initial validation process was conducted using flight data from a single test flight.
While several core functions successfully passed the comparison tests with the MATLAB reference implementation, some functions still require further refinement.
Specifically, the functions \texttt{apply\_rotfiltfilt} and \texttt{downsample\_frd} exhibit deviations that exceed the specified tolerance thresholds when compared to their MATLAB counterparts.
Additionally, \texttt{calculate\_transferfunctions} requires revision in the dynamic gyro low-pass filter configuration, as the current implementation produces runtime errors under certain conditions.

For the ongoing development, a comprehensive testing framework is planned that will validate all library functions against flight data from multiple test flights.
This extended validation will provide a more robust assessment of the Python implementation's reliability and help identify edge cases that may not have been apparent in single-flight testing.
The systematic transfer and validation process established so far provides a solid foundation for completing the migration and ensuring consistency between the MATLAB and Python implementations in future work.

\newpage

\section{Results}

The existing proof-of-concept implementation was restructured into a modular codebase consisting of four distinct classes: \texttt{flight\_data} for Blackbox log management, \texttt{flight\_analyzer} for frequency-domain analysis, \texttt{gyro\_ctrl\_tuning} for system identification and controller analysis, and \texttt{plot\_utils} for visualization. Comprehensive technical documentation in Markdown format was published on GitHub, covering data import, spectral analysis, frequency response estimation, transfer function reconstruction, filter design, and plot interpretation.

Five simulation environments were developed to understand the proof-of-concept code and the methods applied within it. These included a Simulink model of the Betaflight control architecture with inner and outer control loops, all filter types, and a simplified plant model that closely matched experimental behavior, a PID controller characteristics simulation, a frequency response estimation simulation comparing FFT, Welch, and rotational filtering approaches, a spectral analysis simulation demonstrating windowing effects, and a spectrogram generation simulation showing throttle-dependent vibration mapping. These simulations established an understanding of control theory and signal processing techniques required for the analysis workflow.

A three-inch FPV drone was constructed using an aosmini frame, integrated flight controller stack, twelve-pole motors, and three-bladed propellers. The flight controller was flashed with Betaflight version 4.5.0 for chirp mode access. Experimental tuning through iterative chirp flights and offline analysis successfully eliminated motor-induced vibrations and achieved step responses with minimal overshoot. The sensitivity function peak remained below 6 dB across all axes, ensuring adequate stability margins. Flight testing confirmed improvements in propwash handling and maneuver precision compared to default parameters.

Python migration was partially completed. Core library functions including frequency response estimation, spectral analysis, filter design, and closed-loop calculations were transferred and validated against MATLAB implementations. Three functions require refinement: \texttt{apply\_rotfiltfilt} exhibits maximum differences of 0.024 absolute and 0.6\% relative in 0.2\% of data points, \texttt{downsample\_frd} shows systematic deviations across all frequencies, and \texttt{calculate\_transferfunctions} produces runtime errors in dynamic gyro low-pass filter configuration. A validation framework using exported MATLAB test data enables systematic comparison.

\newpage

\section{Discussion and Next Steps}

\subsection{Discussion}

The primary objective of this project was to establish a solid foundation for continued development during the bachelor thesis rather than to deliver a production-ready tool. This foundational focus proved essential, as it enabled the team to develop the necessary expertise in both control theory and practical flight operations before attempting more advanced implementations.

The restructuring of the existing proof-of-concept code into a modular architecture represents a significant technical achievement. The new class-based structure separates concerns effectively, with distinct components for flight data management, frequency-domain analysis, and controller tuning. This separation not only improves code maintainability but also enables independent development of individual modules during future work. The systematic organization of library functions and the introduction of object-oriented design patterns will facilitate rapid implementation of additional features in the bachelor thesis.

Beyond the software architecture, the project required substantial acquisition of domain knowledge. Understanding the Betaflight control system architecture, including the interaction between filter stages and controller components, demanded careful study of both the firmware source code and control-theoretic principles. This knowledge transfer was achieved through a combination of theoretical study and practical validation. The development of multiple simulation environments allowed the team to verify understanding of complex concepts such as rotational filtering, frequency response estimation, and closed-loop transfer function calculation before applying them to real flight data.

The hands-on experience of constructing and tuning an FPV drone proved invaluable for contextualizing the analytical methods. Direct exposure to flight behavior, vibration characteristics, and the practical constraints of parameter adjustment provided insights that cannot be obtained from simulation alone. This experience revealed the gap between theoretical controller design and real-world implementation constraints, such as motor saturation limits, propeller-induced vibrations, and the nonlinear effects of aerodynamic disturbances.

The migration of core functions from MATLAB to Python represents work in progress. While several fundamental algorithms, including closed-loop calculations and transfer function estimation, have been successfully ported and validated against reference implementations, some functions still exhibit numerical discrepancies that require further investigation. The systematic validation approach developed during this phase, using test data from MATLAB as reference outputs, provides a reliable framework for completing the remaining transfers.

However, certain objectives could not be fully realized within the available timeframe. The automatic extraction of flight data directly from Betaflight Blackbox binary logs remains incomplete. Initial attempts using the open-source blackbox decode tool revealed structural incompatibilities in the output format and numerical differences in the decoded data compared to the Betaflight Explorer reference. While these issues are surmountable, addressing them would have diverted resources from more fundamental aspects of the project. The insights gained during this investigation will inform future development efforts.



\subsection{Next Steps}

Future development will focus on three main areas: extending functionality to additional control loops, completing the Python migration, and validating the methods through systematic flight testing.

The feature extension track will introduce offline tuning capabilities for position and altitude hold control loops. These cascaded control structures require more sophisticated identification techniques than the angular rate loops addressed in the current implementation. The analysis must account for the coupling between attitude control and translational dynamics, as well as the influence of aerodynamic effects at different velocities. Implementing these capabilities will require extension of the existing frequency response estimation framework to handle multi-input multi-output systems.

The platform completion track will finalize the migration from MATLAB to Python. This includes resolving the remaining numerical discrepancies in functions such as \texttt{apply\_rotfiltfilt} and \texttt{downsample\_frd}, implementing the missing dynamic filter calculations, and developing a consistent testing framework across all library functions. The Python implementation will be validated against multiple flight datasets to ensure robustness across different drone configurations and flight conditions.

The practical validation track will conduct systematic flight tests to verify the accuracy of the offline analysis and the effectiveness of tuned parameters. This validation will compare predicted system behavior from offline analysis with measured closed-loop performance during flight. Particular attention will be given to assessing how well the linear system identification captures the nonlinear dynamics present in aggressive maneuvers. The results of this validation will inform necessary refinements to the analysis methods and guide the development of robustness criteria for the tuning process.



\subsection{Conclusion}

This project successfully established the foundational knowledge and software infrastructure required for development of a comprehensive offline tuning tool for Betaflight-based FPV drones. The restructured codebase provides a maintainable platform for implementing advanced features, while the systematic validation framework ensures consistency between MATLAB and Python implementations. The combination of theoretical study, simulation-based verification, and practical flight experience yielded a solid understanding of both the Betaflight control architecture and the real-world constraints of FPV drone operation.

The methods implemented demonstrate that offline analysis based on system identification techniques offers a viable alternative to trial-and-error tuning approaches. Frequency response estimation from chirp excitation data enables reconstruction of control loop dynamics, providing objective metrics for evaluating stability margins, disturbance rejection, and tracking performance. However, the transition from analytical capability to practical usability requires further development, particularly in automation of the tuning process and reduction of the control-theoretic knowledge required to interpret results.

The work completed during this project phase positions the bachelor thesis for focused development of the remaining capabilities. The clear separation between data management, analysis algorithms, and visualization components allows parallel development of new features while maintaining code quality. The systematic approach to Python migration provides confidence that the final implementation will maintain numerical accuracy while achieving the accessibility benefits of an open-source platform. Continued validation through flight testing will ensure that the tool delivers reliable performance improvements in real operational scenarios.

\newpage
\printbibliography[title={References}]

\newpage


\subsubsection*{Overview of Generative AI Systems}

\paragraph{OpenAI (2025) — ChatGPT 5.1}
\begin{itemize}
	\item Available online:
	      \href{https://chat.openai.com/}{https://chat.openai.com/}
	\item Functions:
	      \begin{itemize}
		      \item Generation of text suggestions
		      \item Grammar and language correction
		      \item Support in writing LaTeX and MATLAB code
	      \end{itemize}
\end{itemize}

\paragraph{DeepL (2025) — DeepL Translator (Free Version)}
\begin{itemize}
	\item Available online:
	      \href{https://www.deepl.com/translator}{https://www.deepl.com/translator}
	\item Functions:
	      \begin{itemize}
		      \item Translation of text
	      \end{itemize}
\end{itemize}

\paragraph{Microsoft (2025) — Copilot AI Assistant}
\begin{itemize}
	\item Available online:
	      \href{https://copilot.microsoft.com/}{https://copilot.microsoft.com/}
	\item Functions:
	      \begin{itemize}
		      \item Generation of text and summaries
		      \item Integration into Microsoft Office tools such as Word and PowerPoint
		      \item Assistance in software development (e.g., code suggestions)
	      \end{itemize}
\end{itemize}

\paragraph{Anthropic (2025) — Claude AI}
\begin{itemize}
	\item Available online:
	      \href{https://claude.ai/}{https://claude.ai/}
	\item Functions:
	      \begin{itemize}
		      \item Text generation and editing
		      \item Support for research tasks (e.g., summarization and structuring)
		      \item Assistance in programming-related workflows
	      \end{itemize}
\end{itemize}



\newpage

\subsection*{List of Figures}
\vspace*{-3\baselineskip}
\begingroup
\renewcommand{\listfigurename}{}
\renewcommand{\addcontentsline}[3]{}
\listoffigures
\endgroup

\section{Appendix}
\subsection{Project Management}
\subsubsection*{Project Assignment}


% ---------- Aufgabenstellung Seite 1 ----------
\begin{figure}[H]
  \centering
  \includegraphics[page=1,width=0.95\textwidth]{pdf/aufgabenstellung.pdf}
\end{figure}

\clearpage

% ---------- Aufgabenstellung Seite 2 ----------
\begin{figure}[H]
  \centering
  \includegraphics[page=2,width=0.95\textwidth]{pdf/aufgabenstellung.pdf}
\end{figure}

\clearpage

% ---------- Aufgabenstellung Seite 3 ----------
\begin{figure}[H]
  \centering
  \includegraphics[page=3,width=0.95\textwidth]{pdf/aufgabenstellung.pdf}
\end{figure}




\subsubsection*{Project Schedule — Version 0}

This project schedule was created at the beginning of the project.

\begin{figure}[htbp]
	\centering
	\includegraphics[width=0.75\textwidth]{./Images/Zeitplan_V00.png}
	\caption{Project Schedule — Version 0}
	\label{fig:Project Schedule — Version 0}
\end{figure}

\subsubsection*{Project Schedule — Version 1}
This project schedule was created prior to the first milestone meeting and approved during the meeting.

\begin{figure}[htbp]
	\centering
	\includegraphics[width=0.75\textwidth]{./Images/Zeitplan_V01.png}
	\caption{Project Schedule — Version 1}
	\label{fig:Project Schedule — Version 1}
\end{figure}
\newpage

\subsubsection*{Project Schedule — Version 2}
This project schedule was created prior to the second milestone meeting and approved during the meeting.
\begin{figure}[htbp]
	\centering
	\includegraphics[width=0.75\textwidth]{./Images/Zeitplan_V02.png}
	\caption{Project Schedule — Version 2}
	\label{fig:Project Schedule — Version 2}
\end{figure}

\end{document}
